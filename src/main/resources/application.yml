spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

flyway:
  localtions: db/migration

server:
  port: 8085

info:
  app:
    name: shopping-mall-quiz
    description: This is the demo of the homework
    protocol: http
  version: 1.0.0
  contact:
    author: ThoughtWorks School
    email: ""